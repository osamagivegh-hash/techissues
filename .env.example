# ===========================================
# SECURITY NOTICE
# ===========================================
# Never commit .env files with real secrets to version control
# Use environment variables in production (not .env files)
# Generate secrets using: openssl rand -hex 32
# ===========================================

# MongoDB Connection
# REQUIRED: Must be set for database connection
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/techissues?retryWrites=true&w=majority

# JWT Authentication
# REQUIRED: Must be at least 32 characters
# CRITICAL: Do NOT use the example value in production!
# Generate with: openssl rand -hex 32
JWT_SECRET=CHANGE_ME_GENERATE_WITH_openssl_rand_hex_32

# API URL (for frontend)
# In production, set to your actual domain
NEXT_PUBLIC_API_URL=http://localhost:3000

# Site URL (update for production)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Node Environment
NODE_ENV=development

# ===========================================
# Production Example (for reference):
# ===========================================
# MONGODB_URI=mongodb+srv://prod_user:SECURE_PASSWORD@prod-cluster.mongodb.net/techiss-blog
# JWT_SECRET=<64-character-random-hex-string>
# NEXT_PUBLIC_API_URL=https://techiss.store
# NEXT_PUBLIC_SITE_URL=https://techiss.store
# NODE_ENV=production

